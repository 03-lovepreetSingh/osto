// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["your-image-domain.com", "via.placeholder.com"], // replace with actual image domain(s)
  },
};

module.exports = nextConfig;
